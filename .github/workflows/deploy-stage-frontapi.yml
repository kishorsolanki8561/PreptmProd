name: Deploy Stage Front Microservice

on:
  # Push trigger disabled — deploy-stage-backend.yml handles all staging services
  # to avoid race conditions with parallel workflows touching the same systemd units.
  # Use workflow_dispatch below to trigger this service individually if needed.
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 6
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'

      - name: Publish FrontMicroService
        run: |
          dotnet publish backend/FrontMicroService/FrontMicroService.csproj \
            -c Release \
            --no-self-contained \
            -r linux-x64 \
            -o ./publish/frontapi

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SERVER_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.SERVER_HOST }} >> ~/.ssh/known_hosts

      - name: Prepare server directories
        run: |
          ssh -i ~/.ssh/id_rsa ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
            "mkdir -p /root/preptm/BackEnd/Stage/FrontMicroService"

      - name: Stop service before deploy
        run: |
          ssh -i ~/.ssh/id_rsa ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
            "systemctl stop preptm-front-api || true"

      - name: Sync build output to server
        run: |
          rsync -avz --delete \
            -e "ssh -i ~/.ssh/id_rsa" \
            ./publish/frontapi/ \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:/root/preptm/BackEnd/Stage/FrontMicroService/

      - name: Register systemd service
        run: |
          cat > /tmp/preptm-front-api.service << 'UNIT'
          [Unit]
          Description=PreptM Stage Front Microservice
          After=network.target

          [Service]
          WorkingDirectory=/root/preptm/BackEnd/Stage/FrontMicroService
          ExecStart=/usr/bin/dotnet /root/preptm/BackEnd/Stage/FrontMicroService/FrontMicroService.dll
          Restart=always
          RestartSec=10
          KillSignal=SIGINT
          SyslogIdentifier=preptm-front-api
          Environment=ASPNETCORE_ENVIRONMENT=Staging
          Environment=ASPNETCORE_URLS=http://127.0.0.1:5080
          Environment=DOTNET_PRINT_TELEMETRY_MESSAGE=false

          [Install]
          WantedBy=multi-user.target
          UNIT
          scp -i ~/.ssh/id_rsa /tmp/preptm-front-api.service \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:/tmp/preptm-front-api.service
          ssh -i ~/.ssh/id_rsa ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} "
            mv /tmp/preptm-front-api.service /etc/systemd/system/preptm-front-api.service &&
            systemctl daemon-reload &&
            systemctl enable preptm-front-api
          "

      - name: Start service
        run: |
          ssh -i ~/.ssh/id_rsa ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
            "systemctl start preptm-front-api && systemctl status preptm-front-api --no-pager"
